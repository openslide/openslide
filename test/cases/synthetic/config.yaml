base: Aperio/CMU-1.svs
slide: ""
success: true
vendor: synthetic
primary: true
properties:
  # quickhash changes whenever items are added, and is different for each
  # combination of optional features.  Omit.
  openslide.vendor: synthetic
debug:
- synthetic
